#include <stdlib.h>
#include <stdio.h>
#include "interval.h"
#include "artCode.h"

int main(int argc, char** argv)
{
    if(argc != 1){
        exit(EXIT_SUCCESS);
    }
    else{
        exit(EXIT_FAILURE);
    }
}
